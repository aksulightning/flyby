<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- Test APK only: allows the target app to exercise real SAF document calls. -->
        <provider android:name="io.github.aksulightning.flyby.SharedTestProvider"
            android:authorities="io.github.aksulightning.flyby.test.shared"
            android:exported="true" android:grantUriPermissions="true" />
    </application>
</manifest>
